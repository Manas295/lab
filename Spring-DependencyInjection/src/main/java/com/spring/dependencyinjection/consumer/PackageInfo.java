package com.spring.dependencyinjection.consumer;

public class PackageInfo {

	/*
	 * Let’s write a consumer class for the services. 
	 * We will have two consumer classes – one with Spring annotations for autowiring 
	 * and another without annotation and wiring configuration will be provided in the XML configuration file.
	 * 
	 */
	
}
